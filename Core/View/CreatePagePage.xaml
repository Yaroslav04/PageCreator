<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PageCreator.Core.View.CreatePagePage"
             xmlns:model="clr-namespace:PageCreator.Core.Model"
             xmlns:viewmodel="clr-namespace:PageCreator.Core.ViewModel"
             x:DataType="viewmodel:CreatePagePageViewModel"        
             Title="Форма">
    <Grid Margin="5,5,5,5">

        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Entry Grid.Row="0" Grid.Column="0"></Entry>

        <Editor Text="{Binding Text, Mode=TwoWay}" Grid.Row="1" Grid.Column="0"></Editor>
        
    </Grid>
</ContentPage>